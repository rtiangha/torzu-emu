# SPDX-FileCopyrightText: 2019 torzu Emulator Project
# SPDX-License-Identifier: GPL-2.0-or-later

steps:
  - template: ./release-download.yml
  - task: UniversalPackages@0
    displayName: Publish Artifacts
    inputs:
      command: publish
      publishDirectory: '$(Build.ArtifactStagingDirectory)'
      vstsFeedPublish: 'torzu-$(BuildName)'
      vstsFeedPackagePublish: 'main'
      packagePublishDescription: 'Torzu Windows and Linux Executable Packages'